import ProductsHeader from '../../components/ProductsHeader/ProductsHeader';

const Products = () => {
    return (
        <main>
            <ProductsHeader/>            
        </main>
    );
};

export default Products;